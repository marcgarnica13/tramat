"""Shared helpers for {{tramat.project_name}}, seeded by tramat.

Seeded tier: project-owned. Extend these modules rather than duplicating
their logic inside pipelines — the reuse gate reviews against them.
"""
